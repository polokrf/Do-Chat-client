import NotFound from '@/Components/Error/NotFound';
import React from 'react';

const NotFoundMain = () => {
  return (
    <div>
      <NotFound></NotFound>
    </div>
  );
};

export default NotFoundMain;